---
# tasks file for caller

- name: Include role named roletoinclude
  ansible.builtin.include_role:
    name: "{{ roletoinclude }}"

- name: Assert not __caller_override
  ansible.builtin.assert:
    that: not __caller_override
